# Storage Settings
storage:
  # Typ: YAML oder MYSQL
  type: YAML

  # MySQL settings (only when type: MYSQL)
  mysql:
    host: localhost
    port: 3306
    database: minecraft
    username: root
    password: ''

# NPC Default-Settings
defaults:
  hologram-distance: 10.0
  interaction-distance: 5.0
  eye-contact-range: 10.0
